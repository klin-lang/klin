# 153 — `klin_ad9850` chip driver (AD9850 DDS, serial)

**Status:** ✅ published [`klin_ad9850@v0.1.0`](https://github.com/klin-lang/klin_ad9850/releases/tag/v0.1.0);
seed [`@v0.2.0`](../patches/klin_ad9850-v0.2.0/) (typed `u8`/`u32` + numeric `cast`, awaiting upstream tag)
**Depends on:** [020](020-klin-libraries.md), [049](049-remote-imports.md), [154](154-numeric-cast.md);
sibling pattern [149](149-klin-xpt2046.md)

## Verdict

| Question | Answer |
|---|---|
| Change the Klin compiler? | **No** (uses numeric `cast` from [154](154-numeric-cast.md)) |
| Where does the code live? | External: [`klin-lang/klin_ad9850`](https://github.com/klin-lang/klin_ad9850); seed mirrors under `patches/` |
| MCU pins in the driver? | **No** — the app owns `W_CLK` / `FQ_UD` / `DATA` / `RESET` via `Wire` |
| Float for the tuning math? | **No** — integer `FTW = floor(freq · 2³² / ref)` with an `i64` intermediate via `cast` |
| Board pack? | **No** — pinout is app wiring (bit-bang example in the README) |

## Why separate (same reasoning as G-149)

The AD9850 is a bus-agnostic chip: some modules are serial (`D7`/`W_CLK`/`FQ_UD`),
some parallel. Like [`klin_xpt2046`](https://github.com/klin-lang/klin_xpt2046),
the driver stays MCU-agnostic (`Wire` / `ctx`) and the app owns the pins, so it
never pulls a HAL or a specific board into unrelated apps.

## Scope

### `@v0.1.0` (tagged)

Everything on the data path was `i64` — Klin had no integer-width `cast` yet, and
the FTW multiply needs a 64-bit intermediate.

### `@v0.2.0` (seed; tag when upstream is updated)

- `tuning_word` / `freq_from_word` — `u32` Hz and FTW; `cast(i64, …)` for the
  `freq · 2³²` intermediate, then `cast(u32, …)` for the result
- `control_byte` / `word_byte` — return `u8`
- `Wire.write` — `fn(*mut u8, u8)`
- `attach` / `Dev.*` — `ref_hz` / `freq_hz` / `phase` typed (`u32` / `u8`)
- Host `klin test` + `examples/host_smoke.kl`

### Later (not this tag)

- Parallel-load mode (`D0..D7` + `W_CLK`/`FQ_UD`)
- Sweep / ramp helpers
- Phase in degrees (today it is the raw 5-bit code)

## Out of scope

- Board packs, HAL, or a specific `machine_*` dependency
- Hidden allocation, DMA, or float

## Contract (prime rule)

- No Klin GC / hidden heap; the emitted C is the arithmetic you would write by hand.
- Serial load: 40 bits LSB-first (four FTW bytes then the W4 control byte), latched by `FQ_UD`.
- Widths are honest: `u32` FTW, `u8` wire bytes; the only `i64` is the explicit
  cast around `freq * 2^32 / ref`.

## Usage

```sh
# Tagged:
klin get github/klin-lang/klin_ad9850@v0.1.0

# Seed (cast rewrite) until upstream tags v0.2.0:
klin test patches/klin_ad9850-v0.2.0/klin_ad9850
```

Mirrors: [`patches/klin_ad9850-v0.1.0/`](../patches/klin_ad9850-v0.1.0/),
[`patches/klin_ad9850-v0.2.0/`](../patches/klin_ad9850-v0.2.0/).

## Links

- Repo: https://github.com/klin-lang/klin_ad9850
- Tag: [v0.1.0](https://github.com/klin-lang/klin_ad9850/releases/tag/v0.1.0)
- Numeric cast: [154](154-numeric-cast.md)
- Sibling driver: [149](149-klin-xpt2046.md) (`klin_xpt2046`)
